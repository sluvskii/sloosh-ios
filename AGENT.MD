Привет! В предыдущем чате мы успешно связали iOS-приложение `sloosh` с нашим бэкендом NeoMovies API (используем Rust-бэкенд для поиска и выдачи видео-ссылок). Теперь пришло время интегрировать полноценный нативный видеоплеер прямо внутрь приложения, чтобы отказаться от переадресации в Safari.

**Что уже было сделано в рамках подготовки плеера:**
1. Я удалил из `MovieDetailView` старый код с `openURL` (который перекидывал пользователя на сайт API).
2. Теперь по нажатию на кнопку "Смотреть" открывается `PlayerSelectionView` в виде модального окна (sheet) снизу.
3. В `Features/Player/PlayerSelectionView.swift` я сверстал красивый UI-интерфейс для выбора параметров воспроизведения: 
   - Выбор студии озвучки (`Translation`).
   - Выбор сезона и серии (если тип `movie.type == "tv"`).
   - Кнопка "Включить фильм" для полнометражек.
4. Я создал заглушки данных в `Features/Player/PlayerModels.swift`.
5. По нажатию на кнопку воспроизведения открывается полноэкранный `NativePlayerView` с базовым `AVPlayer`, в который сейчас "захардкожена" ссылка на тестовое видео (Big Buck Bunny).

**Наша следующая задача в этом чате:**
Оживить `PlayerSelectionView` и связать его с `NeoMoviesService`.
1. **Запрос переводов и эпизодов:** Нужно добавить в `NeoMoviesService` методы для получения доступных озвучек, сезонов и серий конкретного фильма/сериала из нашего API (по его `kinopoisk_id`).
2. **Динамический UI:** `PlayerSelectionView` должен загружать эти данные при открытии (в `onAppear` или через `.task`) и отображать реальные озвучки вместо заглушек `Translation.mocks`.
3. **Получение видео-потока:** При нажатии на конкретную серию (или фильм) нам нужно делать запрос к нашему API (`/players/...`), чтобы получить прямую ссылку на HLS-стрим (`.m3u8` или `.mp4`).
4. **Улучшение NativePlayerView:** Нужно передавать полученную ссылку в `NativePlayerView` и настроить кастомные контролы или использовать `AVPlayerViewController` для лучшего UX (поддержка AirPlay, картинка-в-картинке, субтитры).

**Важные нюансы проекта:**
- Название приложения пишется с маленькой буквы: **sloosh**.
- Используем системные цвета `.primary`, `.secondary` и наш `SlooshTheme.accent` (кислотно-зеленый).
- Разработка ведется на Windows без Mac, поэтому мы не можем использовать интерактивные превью Xcode, проверяем всё через GitHub Actions (собирается `.ipa`). Пожалуйста, пиши надежный код и сразу исправляй потенциальные ошибки компилятора.

Пожалуйста, начни с изучения `NeoMoviesService.swift` и `PlayerSelectionView.swift`, чтобы понять, как лучше интегрировать новые эндпоинты API.